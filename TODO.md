# TODO

- [x] Determine why `git status` shows uncommitted changes
- [x] Fix `.gitignore` so `backend/target/` build artifacts are ignored
- [x] Optionally remove existing untracked `backend/target/classes/` from working tree


